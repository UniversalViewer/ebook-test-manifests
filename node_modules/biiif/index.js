"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const Directory_1 = require("./Directory");
const Utils_1 = require("./Utils");
const chalk_1 = __importDefault(require("chalk"));
exports.build = async (dir, url, generateThumbs = false, virtualName) => {
    console.log(chalk_1.default.white('started biiifing ' + dir));
    // validate inputs
    const exists = await Utils_1.Utils.fileExists(dir);
    if (!exists) {
        throw new Error('Directory does not exist');
    }
    if (!url) {
        throw new Error('You must pass a url parameter');
    }
    const directory = new Directory_1.Directory(dir, url, generateThumbs, virtualName);
    await directory.read();
    console.log(chalk_1.default.white('finished biiifing ' + dir));
};
